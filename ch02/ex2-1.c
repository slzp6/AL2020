/* code: ex2-1.c   (v1.20.00) */
#include <stdio.h>

int main () {
  int x, y;

  x = 500;
  y = 300;

  printf ("X = %d\n", x);
  printf ("Y = %d\n", y);

  if (x > y)
    printf ("X is greater than Y.\n");


  return 0;
}
